Flask>=3.0
anthropic>=0.69
requests>=2.32
pytest>=8.0
# QR-Code beim Start (Handy-Zugang im WLAN). Reines Python, keine weiteren Abhängigkeiten.
qrcode>=7.4
# Für den optionalen Abo-Weg (Rechenleistung über das Claude-Abo statt API-Schlüssel).
claude-agent-sdk>=0.1
